<h1>All users</h1>

<% if @users.any? %>
  <% @users.each do |user| %>
    <p>Id: <%= user.id %></p>
    <p>Login: <%= user.login %></p>
    <p>Date: <%= user.created_at %></p>
  <% end %>
<% else %>
  <p>No users!</p>
<% end %>
